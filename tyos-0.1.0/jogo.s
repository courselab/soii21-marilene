	.file	"jogo.c"
	.code16gcc
	.text
	.globl	jogo
	.type	jogo, @function
jogo:
.LFB0:
	.cfi_startproc
#APP
# 5 "jogo.c" 1
	        mov   $0x00, %ah           ;        mov   $0x13, %al           ;        int $0x10                   ;        movw   $0xa000, %cx         ;        movw   %cx, %es            ;        mov    $0x0, %si            ;        mov    $0x00, %bx           ;        mov    $0x0a, %al           ;        movb %al, %es:(%bx, %si) ; fundo_azul1:                                 mov    $0x35, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x3200, %si          ;        jne fundo_azul1              ; fundo_azul2:                                 mov    $0x4d, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x8340, %si          ;        jne fundo_azul2              ; fundo_azul3:                                 mov    $0x4c, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0xd480, %si          ;        jne fundo_azul3              ; grama:                                        mov    $0x02, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0xe100, %si          ;        jne grama                    ; chao:                                        mov    $0xbb, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0xfa00, %si          ;        jne chao                     ;        mov    $0x528, %si          ; nuvem1_a:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x532, %si           ;        jne nuvem1_a                 ;        mov    $0x668, %si          ; nuvem1_b:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x672, %si           ;        jne nuvem1_b                 ;        mov    $0x7a8, %si          ; nuvem1_c:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x7b2, %si           ;        jne nuvem1_c                 ;        mov    $0x8e8, %si          ; nuvem1_d:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x8f2, %si           ;        jne nuvem1_d                 ;        mov    $0xa28, %si          ; nuvem1_e:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0xa32, %si           ;        jne nuvem1_e                 ;        mov    $0xb5e, %si          ; nuvem2_a:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0xb7c, %si           ;        jne nuvem2_a                 ;        mov    $0xc9e, %si          ; nuvem2_b:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0xcbc, %si           ;        jne nuvem2_b                 ;        mov    $0xdde, %si          ; nuvem2_c:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0xdfc, %si           ;        jne nuvem2_c                 ;        mov    $0xf1e, %si          ; nuvem2_d:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0xf3c, %si           ;        jne nuvem2_d                 ;        mov    $0x105e, %si         ; nuvem2_e:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x107c, %si          ;        jne nuvem2_e                 ;        mov    $0x119e, %si         ; nuvem2_f:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x11bc, %si           ;        jne nuvem2_f                 ;        mov    $0x12de, %si         ; nuvem2_g:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x12fc, %si          ;        jne nuvem2_g                 ;        mov    $0x1414, %si          ; nuvem3_a:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x1446, %si           ;        jne nuvem3_a                 ;        mov    $0x1554, %si          ; nuvem3_b:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x1586, %si           ;        jne nuvem3_b                 ;        mov    $0x1694, %si          ; nuvem3_c:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x16c6, %si           ;        jne nuvem3_c                 ;        mov    $0x17d4, %si          ; nuvem3_d:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x1806, %si           ;        jne nuvem3_d                 ;        mov    $0x1914, %si          ; nuvem3_e:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x1946, %si           ;        jne nuvem3_e                 ;        mov    $0x1a54, %si         ; nuvem3_f:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x1a86, %si          ;        jne nuvem3_f                 ;        mov    $0x1b94, %si         ; nuvem3_g:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x1bc6, %si           ;        jne nuvem3_g                 ;        mov    $0x1cd4, %si         ; nuvem3_h:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x1d06, %si          ;        jne nuvem3_h                 ;        mov    $0x1e14, %si         ; nuvem3_i:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x1e46, %si           ;        jne nuvem3_i                 ;        mov    $0x1f54, %si         ; nuvem3_j:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x1f86, %si          ;        jne nuvem3_j                 ;        mov    $0x120c, %si          ; nuvem4_a:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x1216, %si           ;        jne nuvem4_a                 ;        mov    $0x134c, %si          ; nuvem4_b:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x1356, %si           ;        jne nuvem4_b                 ;        mov    $0x148c, %si          ; nuvem4_c:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x1496, %si           ;        jne nuvem4_c                 ;        mov    $0x15cc, %si          ; nuvem4_d:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x15d6, %si           ;        jne nuvem4_d                 ;        mov    $0x170c, %si          ; nuvem4_e:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x1716, %si           ;        jne nuvem4_e                 ;        mov    $0x1842, %si          ; nuvem5_a:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x1860, %si           ;        jne nuvem5_a                 ;        mov    $0x1982, %si          ; nuvem5_b:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x19a0, %si           ;        jne nuvem5_b                 ;        mov    $0x1ac2, %si          ; nuvem5_c:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x1ae0, %si           ;        jne nuvem5_c                 ;        mov    $0x1c02, %si          ; nuvem5_d:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x1c20, %si           ;        jne nuvem5_d                 ;        mov    $0x1d42, %si          ; nuvem5_e:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x1d60, %si           ;        jne nuvem5_e                 ;        mov    $0x1e82, %si         ; nuvem5_f:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x1ea0, %si          ;        jne nuvem5_f                 ;        mov    $0x1fc2, %si         ; nuvem5_g:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x1fe0, %si           ;        jne nuvem5_g                 ;        mov    $0x20f8, %si          ; nuvem6_a:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x212a, %si           ;        jne nuvem6_a                 ;        mov    $0x2238, %si          ; nuvem6_b:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x226a, %si           ;        jne nuvem6_b                 ;        mov    $0x2378, %si          ; nuvem6_c:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x23aa, %si           ;        jne nuvem6_c                 ;        mov    $0x24b8, %si          ; nuvem6_d:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x24ea, %si           ;        jne nuvem6_d                 ;        mov    $0x25f8, %si          ; nuvem6_e:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x262a, %si           ;        jne nuvem6_e                 ;        mov    $0x2738, %si         ; nuvem6_f:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x276a, %si          ;        jne nuvem6_f                 ;        mov    $0x2878, %si         ; nuvem6_g:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x28aa, %si           ;        jne nuvem6_g                 ;        mov    $0x29b8, %si         ; nuvem6_h:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x29ea, %si          ;        jne nuvem6_h                 ;        mov    $0x2af8, %si         ; nuvem6_i:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x2b2a, %si           ;        jne nuvem6_i                 ;        mov    $0x2c38, %si         ; nuvem6_j:                                    mov    $0x0f, %al           ;        movb %al, %es:(%bx, %si) ;        inc    %si                  ;        cmp   $0x2c6a, %si          ;        jne nuvem6_j                 ;        ret                         ;
# 0 "" 2
#NO_APP
	nop
	ud2
	.cfi_endproc
.LFE0:
	.size	jogo, .-jogo
	.ident	"GCC: (Ubuntu 9.3.0-17ubuntu1~20.04) 9.3.0"
	.section	.note.GNU-stack,"",@progbits
